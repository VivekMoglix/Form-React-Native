const INITIAL_STATE = {
  value: '',
  label: '',
  labelColor: '',
  textColor: '',
  borderColor: '',
};

export default function textFieldReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
  }
}
